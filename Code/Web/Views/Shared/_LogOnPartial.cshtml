@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@ViewData["logged-in-name"]</strong>!
    [ @Html.ActionLink("log off", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("log on", "LogOn", "Account") ]
}
